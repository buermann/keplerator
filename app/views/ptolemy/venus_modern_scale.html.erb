<script>
  window.onload = function(){
    var universe = new Universe({ sun:{},earth:{type:'center'},venus:{}  });
    universe.planets.sun.drawDeferent = false;
    universe.planets.sun.eccentric(0);
    universe.max_planet_size = 15;
    //universe.planets.sun.aus  = 120;
    //universe.planets.venus.aus = 60; // unit distances
    <%= render 'venus' %>

    universe.setup(); 
  }
</script>

<div id='lefttext'>
  If you build Ptolemy's model for Venus with the modern relative distances between bodies,
  it looks like a geoheliocentric model and predicts a full Venetian phase when Venus is opposite the Sun from the Earth.
</div>
